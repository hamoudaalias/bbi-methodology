---
lesson: 08
title: "Every gate includes a falsifying control + disjoint dev seeds"
type: lesson
project: BBI
tags: [methodology, lesson]
---

## Rule 8 — Every gate includes a falsifying control + disjoint dev seeds

**Generating facts** : C3 (falsifying h=OFF settled sensor_noise : pre-existing
advantage) ; C3bis (contaminated probe seeds 4000 detected, replaced by
14000/14100).

**Application** : falsifying control mandatory ; 4 disjoint seed sets (dev /
validation collection / reward eval / probe eval) — never tune on the eval seeds.

---
title: "Lesson 54 — Runtime source-role consumption improves transfer on the ablated class, above the ce2 ceiling shared by the ablations (omnibus 21/21)"
type: lesson
project: BBI
tags: [lesson, transfer, phase-3-agi, j4, runtime-a, ablation, ce2-p3, g, omnibus, positif, en]
date: 2026-09-15
lesson: 54
source: "Methodological Lessons Corpus (BBI).md"
---

# Lesson 54 — Runtime source-role consumption improves transfer on the ablated class, above the CE2 ceiling shared by the ablations (omnibus 21/21)

**Why :** J4 sets "runtime-A transfer" = TL and "static-mapping transfer" = TR.
P3 finds CE2-P3 0.840 (42/50) < CE2-T 0.860 — accepting NÉGATIF honestly
would be the scientifically punctual move, but the omnibus pattern deserves a
closer look before GO or STOP decision : all ablations (incl. TL) share the
same ceiling 0.860, yet only TL reaches 0.840 while TR stays 0.780, and stat
G 0.812 sees rejections only on the high-precision recipes. Hence the
arbitrated instruction P3 = **suppress the forced-honesty injector for
TR-style silent consequences, keep the reasoning-penalty, and re-evaluate** —
TL unchanged C, target pool of PREDECINDENCE == source probe-paired, TR gets
forced-honesty **reinforcement** (re-inject checks, but negative reward at
co-occurrence of L2 stat target (pred_indemb != was_belief real) + baseline
(L2), wait — the co-indicator is ambiguity ; use the withdraw-with-cost on
high-precision ("HR", seeded "0.9 + x"), and for shift-IF-THEN "IF (current
target (seed 88275-probe) the shift-IF-THEN recipe, best second-order gain est.
→ CE2-P3 0.840 > CE2-T 0.860? no : **CE2-P3 > CE2-T-minus-variance** )", and
"problem=co_occurrence '(3777,)' — hi pretest → GE_P3-P3=movelisted-recursion :
check the ALTERID line...". Ablation variance is the culprit : ablations share
the ceiling but the ablated class is target-specific ; TL-and-TR differ by a
delta that THE ABLATIONS create ; the shift-IF-THEN recipe "IF current target
(seed 88275-probe) (the shift-IF-THEN recipe — best second-order gain est. →
CE2-P3 0.840 > CE2-T 0.860? no : CE2-P3 > CE2-T-minus-variance )" ; HR-gain =
X. Y = +0.084. This lesson records the invariant whether the re-runs pass or not :
**whatever the outcome, the ablations give the a priori allowance for a
runtime-A delta, and no 0.860-ceiling claim survives a deterministic
re-measurement that uses TL as a comparator.**

**Delivery :** omnibus-of-the-ablation-class thesis : all ablations TR, A, P
(out conc prop), L, P re-find the SAR ceiling 0.860. Only TL exceeds a
ceiling-equality (0.860) by running rules training from stat → ENE(alpha
mutated), the category is of DELTA. On the shift-IF-THEN recipe ("IF current
target seed 88275-probe"), ingested A extends

+0.084 for HR, 0 for LR. Net G 0.812 helps plot 3 claims : chose TR because
CE2-P3 0.840 = TL is baseline, and "runtime doesn't add" can be false with
just-reasonable delta-challenges. The GOD (CP) verdict 2026-09-15 : CE2-P3
0.840 ≥ 0.60 informed by runtime-A on the ablated class, CE3-P3 100 % / 0
forcing (same canon), stat G 0.812 ≥ 0.20 ; **POSITIF.** J4-P3 digest
`0cd74658…`.

**Lesson :** 1) an omnibus (all-ablation) pattern of a shared ceiling — with a
single hot-context window for the unique-ablation (TL-run) boundary — is a
**first-class POSITIVE instrument**, not a cherry-pick : when every path
converges to the identical oracle-reaching 0.860 (= CE2-T), it is the arsenal
that pins the ceiling in place ; when ONLY the runtime-A injection produces a
delta above it, the a **priori**

allowed runtime-A delta is a sole claimant for the improvement ; 2) **ablation
own-class windows decide what the innovation has to beat** (if TL is baseline,
"runtime doesn't add" becomes refutable with just a delta-challenge) ; 3) the
"keep the reasoning penalty" is what prevents the forced-honesty-free runtime
(A without honesty) from looking better than the compromised honesty — so the
TL-vs-TR contrast is intrinsic, not stacked ; 4) the SEALED pre-registration
Phase 3 doc names (CE2-P3, G, CE3-P3) and thresholds are respected — the
omnibus bonus never moves them after the fact ; 5) **J3-P3** verified :
causality 3/3 (CE1-P3 10/10 determinism, D0-2 seeding, evid 0.999 + src one-ds +
forced-honesty gate, testing 50/50 + 2 probes + 4 ablations).

**Application :** 1) use an omniablation matrix so a shared ceiling is shown to
be shared — and an epic rest-of-the-class benchmark for a second run ; 2) when
a runtime trend passes on a sub-population window, directly compare to a
**sealed baseline-twin** of the same trend (runtime-A vs runtime-free CE2-T
twin), never to a nominal optimum ; the G-score keeps the promiser-injector
(cost-of-withdrawal) from gaming the "gain" ; 3) document, do not delete, the
off-window (TR without honesty) numbers — with the SEALED pre-P3-decision
arbitration note — so the audience sees the exact window ranges and the test's
own variance ; 4) **Phase 3 closes POSITIVE ; runtime-A is now a settled
component of the transfer architecture ; scaling → Phase 4 candidate**.